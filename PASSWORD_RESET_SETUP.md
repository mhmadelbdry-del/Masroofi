# إعداد استعادة كلمة مرور Masroofi

أضيفت شاشة «نسيت كلمة المرور؟» وشاشة تعيين كلمة مرور جديدة. إرسال الرسالة يتم عبر Resend من الخادم، ولا يتم وضع مفتاح البريد داخل الواجهة أو داخل APK.

## متغيرات Vercel

أضف المتغيرين التاليين في مشروع Vercel ضمن Environment: Production:

```text
RESEND_API_KEY=re_xxxxxxxxx
EMAIL_FROM=Masroofi <noreply@your-domain.com>
```

يجب أن يكون `RESEND_API_KEY` من نوع Secret. قيمة `EMAIL_FROM` يجب أن تستخدم نطاقًا موثقًا في Resend. أثناء الاختبار يمكن استخدام عنوان Resend التجريبي، وقد تكون الرسائل التجريبية مقيدة بعنوان حساب Resend.

## إنشاء المفتاح

1. أنشئ حسابًا في [Resend](https://resend.com/).
2. افتح API Keys وأنشئ مفتاحًا جديدًا بصلاحية إرسال البريد.
3. انسخ المفتاح مرة واحدة فقط وضعه في Vercel كقيمة `RESEND_API_KEY`.
4. وثّق نطاق الإرسال في Resend، ثم ضع عنوانًا مثل `Masroofi <noreply@your-domain.com>` في `EMAIL_FROM`.
5. أعد نشر Production من Vercel بعد حفظ المتغيرات.

## سلوك التطبيق

من شاشة الدخول يضغط المستخدم «نسيت كلمة المرور؟»، يكتب بريده، ثم تصله رسالة تحتوي على رابط مؤقت. الرابط يفتح شاشة تعيين كلمة مرور جديدة. بعد النجاح تُلغى الجلسات القديمة ويعود المستخدم إلى شاشة الدخول.

لا تضع مفتاح Resend في GitHub أو داخل `src` أو داخل APK. إذا لم تتم إضافة `RESEND_API_KEY` فلن يستطيع الخادم إرسال الرسائل، وستظهر رسالة بأن إعداد البريد غير مكتمل.
